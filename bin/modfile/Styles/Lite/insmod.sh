#!/usr/bin/env bash
# DeadZone Lite style mods — minimal/base build, no extra patches.
# Called after base mods run, before repacking.
work_dir=${work_dir:-$(pwd)}
echo "[STYLE] Lite: base mods only — no additional patches applied."
